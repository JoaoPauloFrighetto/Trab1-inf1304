# Build do Maven (gerado dentro do Docker; não precisa ser versionado)
target/

# Ferramenta local usada só para gerar os documentos .docx/.txt (não faz parte da entrega)
docs/gerar/node_modules/
docs/gerar/package.json
docs/gerar/package-lock.json

# Arquivos de trava do Word (abertos ao editar os .docx)
~$*.docx
~$*.doc

# Diversos
.DS_Store
Thumbs.db
*.bak
