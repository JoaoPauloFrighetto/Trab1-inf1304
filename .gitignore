# Build do Maven (gerado dentro do Docker; não precisa ser versionado)
target/

# Logs gerados em tempo de execução (mantemos só logs/exemplo-logs.txt, versionado)
logs/*.log
logs/*.jsonl
logs/execucao-*/

# Arquivos de trava do Word (abertos ao editar os .docx)
~$*.docx
~$*.doc

# Diversos
.DS_Store
Thumbs.db
*.bak
